import { google } from "@next/font/google";

const oxygen = google({
  family: "Oxygen",
  subsets: ["latin"], // Add other subsets if needed
  weights: ["300", "400", "700"], // Include desired weights
});
